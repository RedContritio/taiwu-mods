# 实际数值表

数据来源：反编译代码中的 `RelationsRelatedConstants`、`FixedActionConstants`、`AiHelper.Relation` 等。
数组索引对应行为类型：`[0:刚正, 1:仁善, 2:中庸, 3:叛逆, 4:唯我]`

---

## 第一部分：常量数组完整列表

### RelationsRelatedConstants（关系相关常量）

#### 数组型常量

| 常量名 | 刚正 | 仁善 | 中庸 | 叛逆 | 唯我 | 说明 |
|--------|------|------|------|------|------|------|
| `IncestSexRelationChanceFactor` | 5 | 10 | 15 | 25 | 20 | 近亲恋爱概率乘数(%) |
| `StartAdoreChanceChangePerAdored` | -50 | -40 | -30 | -10 | -20 | 目标每多一个爱慕者的概率增量 |
| `BecomeEnemyHappinessChange` | 0 | -5 | -10 | 5 | 0 | 结仇心情变化 |
| `SeverEnemyHappinessChange` | 0 | 5 | 10 | -5 | 0 | 解仇心情变化 |
| `SeverEnemyNotMutuallyChance` | 30 | 10 | 20 | 50 | 40 | 结仇/解仇双向概率(%) |
| `ConfessLoveSucceedHappinessChange` | 5 | 10 | 5 | 10 | 5 | 表白成功心情 |
| `ConfessLoveSucceedNeedForSexChance` | 0 | 10 | 20 | 40 | 30 | 表白成功后发生性行为概率(%) |
| `ConfessLoveFailedHappinessChange` | -10 | -10 | -10 | -10 | -10 | 表白失败心情 |
| `BecomeSingleNeedNewLoveChance` | 0 | 10 | 20 | 40 | 30 | 恢复单身后寻找新恋人概率(%) |
| `ConfessLoveFailedNeedForRapeChance` | 0 | 0 | 5 | 15 | 10 | 表白失败后强制行为概率(%) |
| `ConfessLoveOrProposeFailedBecomeEnemyChance` | 5 | 0 | 10 | 20 | 15 | 表白/求婚失败后变仇敌概率(%) |
| `BreakupBecomeEnemyChance` | 50 | 30 | 20 | 40 | 60 | 分手后变仇敌概率(%) |
| `BreakupMutuallyChance` | 30 | 10 | 20 | 50 | 40 | 分手时双方同意概率(%) |
| `BreakupHappinessChange` | 0 | -5 | -10 | -5 | 0 | 分手心情 |
| `ProposeMarriageSucceedHappinessChange` | 15 | 15 | 15 | 15 | 15 | 求婚成功心情 |
| `ProposeMarriageFailHappinessChange` | -5 | -10 | -5 | -10 | -5 | 求婚失败心情 |
| `BecomeFriendHappinessChange` | 10 | 10 | 5 | 0 | 0 | 结友心情 |
| `SeverFriendHappinessChange` | -5 | -5 | -5 | -5 | -5 | 断友心情 |
| `BecomeSwornOrAdoptedFamilyHappinessChange` | 15 | 15 | 10 | 5 | 5 | 结义/收养心情 |
| `SeverSwornOrAdoptedFamilyHappinessChange` | -15 | -15 | -15 | -15 | -15 | 断义/断收养心情 |
| `SeverHusbandOrWifeHappinessChange` | -15 | -20 | -15 | -20 | -15 | 离婚心情 |
| `BecomeMentorHappinessChange` | 0 | 5 | 10 | -5 | 0 | 拜师心情 |
| `AdoptedBecomeEnemy` | 70 | 50 | 40 | 60 | 80 | 断义亲后变仇敌概率(%) |
| `FriendshipBreakBecomeEnemy` | 30 | 10 | 5 | 25 | 40 | 断友后变仇敌概率(%) |

#### 标量常量

| 常量名 | 值 | 说明 |
|--------|-----|------|
| `ConfessLoveFavorabilityChange` | +3000 | 表白成功好感度变化（失败为 -3000） |
| `BreakupFavorabilityChange` | -12000 | 恋人分手好感度 |
| `ProposeMarriageSucceedFavorabilityChange` | +12000 | 求婚成功好感度 |
| `ProposeMarriageFailFavorabilityChange` | -3000 | 求婚失败好感度 |
| `BecomeSwornOrAdoptedFamilyFavorabilityChange` | +12000 | 结义/收养好感度 |
| `FriendshipFavorabilityChange` | +3000 | 结为好友好感度 |
| `FriendshipBreakFavorabilityChange` | -8000 | 断绝友谊好感度 |
| `SeverSwornOrAdoptedFavorabilityChange` | -16000 | 断义/断收养好感度 |
| `SeverHusbandOrWifeFavorabilityChange` | -20000 | 离婚好感度 |
| `BecomeMentorFavorabilityChange` | +3000 | 拜师好感度 |
| `BreakupAlertnessChange` | +10000 | 分手涉及太吾时警觉度 |
| `AdoptedAlertnessChange` | +20000 | 断义亲涉及太吾时警觉度 |
| `FriendshipBreakAlertnessChange` | +8000 | 断友涉及太吾时警觉度 |
| `DivorceAlertnessChange` | +30000 | 离婚涉及太吾时警觉度 |

### FixedActionConstants（固定行为常量）

#### 数组型常量

| 常量名 | 刚正 | 仁善 | 中庸 | 叛逆 | 唯我 | 说明 |
|--------|------|------|------|------|------|------|
| `BoyAndGirlFriendMakeLoveBaseChance` | 5 | 10 | 15 | 25 | 20 | 恋人性行为基础概率(%) |
| `RapeBaseChance` | 0 | 0 | 3 | 9 | 6 | 强制行为基础概率(%) |
| `ReleaseKidnappedCharResistanceThreshold` | 75 | 50 | 75 | 50 | 75 | 释放囚犯抗性阈值 |
| `ReleaseKidnappedCharChance` | 20 | 50 | 30 | 40 | 10 | 释放囚犯概率(%) |
| `JoinGroupBaseChance` | 10 | 15 | 20 | 0 | 5 | 加入队伍基础概率(%) |

#### 标量常量

| 常量名 | 值 | 说明 |
|--------|-----|------|
| `RapeImpregnateChance` | 20 | 强制行为怀孕概率(%) |
| `MutualMakeLoveImpregnateChance` | 60 | 合法性行为怀孕概率(%) |
| `FertilityDivisor` | 20 | 太吾生育力除数（NPC用40） |
| `RapeFavorabilityChange` | -30000 | 强制行为好感度 |
| `NpcGroupSoftMaxCount` | 5 | NPC队伍软上限 |

#### 队伍相关数组（7元素，按关系类型索引）

| 常量名 | 值 | 说明 |
|--------|-----|------|
| `JoinGroupFavorabilityReq` | [4, 3, 3, 3, 4, 4, 5] | 入队好感度要求 |
| `StayInGroupMonth` | [12, 6, 12, 12, 6, 6, 6] | 驻队最低月数 |
| `LeaveGroupChancePerMonth` | [10, 30, 15, 15, 10, 30, 30] | 每月离队概率(%) |
| `MaxLeaveGroupChance` | [60, 90, 90, 90, 60, 90, 90] | 离队概率上限(%) |

---

## 第二部分：月度AI关系处理伪代码

### 关系类型位掩码

| 位掩码 | 关系类型 | AI模板ID（开始） | AI模板ID（结束） |
|--------|----------|-----------------|-----------------|
| 0x8000 (32768) | 仇敌 | 0 | 1 |
| 0x4000 (16384) | 爱慕/两情相悦 | 2 (爱慕) / 3 (表白) | 4 (分手) |
| 0x2000 (8192) | 好友 | 6 | 7 |
| 0x0400 (1024) | 夫妻 | 5 | 12 |
| 0x0200 (512) | 结义 | 8 | 9 |
| 0x0080 (128) | 义子女 | 11 | - |
| 0x0040 (64) | 义父母 | 10 | - |

### 2.1 TripleStartRelationChance（三倍概率判定）

```
L540049  function TripleStartRelationChance(aiRelationsTemplateId):
L540051    tripleAdoreLocation = Character.TripleAdoreLocation   // 全局爱慕增强坐标
L540052    tripleHateLocation  = Character.TripleHateLocation    // 全局仇恨增强坐标
L540053    if aiRelationsTemplateId == 2 (爱慕)
L540053      AND tripleAdoreLocation.IsValid()
L540053      AND tripleAdoreLocation.AreaId == self.location.AreaId:
L540055        areaSize = Map.GetAreaSize(self.location.AreaId)
L540056        coord1 = IndexToCoordinate(tripleAdoreLocation.BlockId, areaSize)
L540057        coord2 = IndexToCoordinate(self.location.BlockId, areaSize)
L540058        return ManhattanDistance(coord1, coord2) <= 2
L540060    if aiRelationsTemplateId == 0 (结仇)
L540060      AND tripleHateLocation.IsValid()
L540060      AND tripleHateLocation.AreaId == self.location.AreaId:
L540062        areaSize = Map.GetAreaSize(self.location.AreaId)
L540063        coord3 = IndexToCoordinate(tripleHateLocation.BlockId, areaSize)
L540064        coord4 = IndexToCoordinate(self.location.BlockId, areaSize)
L540065        return ManhattanDistance(coord3, coord4) <= 2
L540067    return false
```

### 2.2 GetStartOrEndRelationTarget（选择关系目标）

```
L540070  function GetStartOrEndRelationTarget(random, aiTemplateId, candidateList, selfPersonalities):
L540072    if candidateList.Count == 0: return null
L540076    multiplier = TripleStartRelationChance(aiTemplateId) ? 3 : 1
L540077    aiRelationConfig = AiHelper.Relation.GetAiRelationConfig(aiTemplateId)
L540078    if NOT CheckChangeRelationTypeChance(random, selfPersonalities, config.PersonalityType, multiplier):
L540080      return null
L540082    targetId = candidateList.GetRandom(random)     // 从候选列表中随机选一个
L540083    target = Character.GetElement(targetId)
L540084    relation = GetRelation(self.id, targetId)
L540085    sectFavorability = Organization.GetSectFavorability(self.OrgTemplateId, target.OrgTemplateId)
L540086    chance = GetStartOrEndRelationChance(config, self, target, relation.RelationType,
                                                sectFavorability, multiplier)
L540087    return random.CheckProb(chance, 10000) ? target : null
             // 注意: 概率检定基数为 10000，非百分比
```

### 2.3 GetStartOrEndRelationChance（计算关系变更概率）

```
L558706  function GetStartOrEndRelationChance(cfg, selfChar, targetChar, curRelType, sectFavor, multiplier=1):
L558708    selfBehavior  = selfChar.GetBehaviorType()
L558709    targetBehavior = targetChar.GetBehaviorType()
L558710    selfFame  = selfChar.GetFameType()
L558711    targetFame = targetChar.GetFameType()
L558712    adjust = 0
           // --- 门派与行为/声望调整 ---
L558713    if cfg.NoncontradictoryBehaviorAjust != 0 AND 行为不矛盾(selfBehavior, targetBehavior):
L558715      adjust += cfg.NoncontradictoryBehaviorAjust
L558717    if cfg.NoncontradictoryFameAjust != 0 AND 声望不矛盾(selfFame, targetFame):
L558719      adjust += cfg.NoncontradictoryFameAjust
L558721    if cfg.EnemySectMemberAdjust != 0 AND sectFavor == -1 (敌对门派):
L558723      adjust += cfg.EnemySectMemberAdjust
L558725    elif cfg.FriendlySectMemberAdjust != 0 AND sectFavor == 1 (友好门派):
L558727      adjust += cfg.FriendlySectMemberAdjust

           // --- 模板特殊处理 ---
L558729    percentMul = 100
L558730    relatedChars = GetRelatedCharacters(selfChar.id)
L558731    switch cfg.TemplateId:
L558733      case 0 (结仇):
L558734        if self或target有特性741: percentMul *= 10      // 10倍概率
L558739      case 2 (爱慕):
L558741        adoreCount = relatedChars.Adored.GetCount()
L558742        if adoreCount > 0:
L558744          perAdored = StartAdoreChanceChangePerAdored[selfBehavior]
L558745          featureFactor = CalcAdoreMultiplePeopleChanceFactor(selfChar)
L558746          percentMul += adoreCount * perAdored * featureFactor / 100
L558748        if self或target有特性740: percentMul *= 10      // 10倍概率
L558752        if self有特性685 AND target非太吾知己: return 0   // 专情特性阻止
L558758      case 3 (表白):
L558759        if self有"想要表白"目标(Goal 251, 0x4000):
L558761          percentMul = percentMul * 150 / 100            // 1.5倍
L558763        if self有特性685 AND target非太吾知己: return 0
L558768      case 4 (分手):
L558769        percentMul += 20 * (relatedChars.Adored.Count - 1) // 爱慕者越多越想分手
L558771      case 8 (结义):
L558772        percentMul -= 20 * relatedChars.SwornBrothersAndSisters.Count // 已有义亲越多概率越低
L558773        if self有"想要结义"目标(Goal 251, 0x200):
L558775          percentMul = percentMul * 150 / 100
L558777        if 已有爱慕关系(curRelType & 0x4000):
L558779          adjust -= 2000                                 // 恋人不想结义

           // --- 基础概率取值（按当前关系类型分支） ---
L558783    if 直系血亲 OR 夫妻关系:
             adjust += cfg.Probability[selfBehavior].BloodRelationsProb
           elif 非血亲家族关系 OR 师徒/同门:
             adjust += cfg.Probability[selfBehavior].SwornOrAdoptiveRelationsProb
           else:
             adjust += cfg.Probability[selfBehavior].DefaultProb

           // --- 最终概率 ---
L558784    return max(0, adjust * percentMul * multiplier / 100)
             // 结果基数为万分之，供 CheckProb(chance, 10000) 使用
```

### 2.4 PeriAdvanceMonth_RelationsUpdate（月度主循环）

```
L540130  function PeriAdvanceMonth_RelationsUpdate(context, charSet):
L540132    if self.AgeGroup == 0 (幼年) OR self.IsActingCrazy: return
L540136    mod = new PeriAdvanceMonthRelationsUpdateModification(self)
L540137    hasChanges = false
L540138    newlyMetList = new List<Character>()

           // --- 获取候选角色列表 ---
L540139    canStart = context.CurrBlockCanStartRelationChars.Occupy()
L540140    canEnd   = context.CurrBlockCanEndRelationChars.Occupy()
L540141    GetPotentialRelatedCharactersInSet(canStart, canEnd, newlyMetList, self, charSet)
L540142    if newlyMetList.Count != 0:
L540144      mod.NewlyMetCharacters = newlyMetList
L540145      hasChanges = true

L540147    selfPersonalities = self.GetPersonalities()
L540148    random = context.Random

           // ===== 结仇 (TemplateId=0, Bitmask=0x8000) =====
L540149    target = GetStartOrEndRelationTarget(random, 0, canStart.Enemies, selfPersonalities)
L540150    if target != null:
L540151      hasChanges = true
L540153      targetBehavior = target.GetBehaviorType()
L540154      isMutual = random.CheckPercentProb(SeverEnemyNotMutuallyChance[targetBehavior])
             // isMutual=true 表示双向结仇
L540160      mod.NewRegularRelations.Add(target, 0x8000, isMutual)

           // ===== 解仇 (TemplateId=1, Bitmask=0x8000) =====
L540162    target = GetStartOrEndRelationTarget(random, 1, canEnd.Enemies, selfPersonalities)
L540163    if target != null:
L540164      hasChanges = true
L540166      targetBehavior = target.GetBehaviorType()
L540167      isMutual = random.CheckPercentProb(SeverEnemyNotMutuallyChance[targetBehavior])
             // isMutual=true 表示对方也解除（item6 在后续 complement 中使用, 注意字段名是 targetStillHasRelation）
L540173      mod.RemovedRegularRelations.Add(target, 0x8000, isMutual)

           // ===== 心生爱慕 (TemplateId=2, Bitmask=0x4000) =====
L540175    target = GetStartOrEndRelationTarget(random, 2, canStart.Adored, selfPersonalities)
L540176    if target != null:
L540178      selfToTarget = GetRelation(self.id, target.id)
L540179      targetToSelf = GetRelation(target.id, self.id)
L540180      successRate = GetStartRelationSuccessRate_Adored(self, target, selfToTarget, targetToSelf)
L540181      if random.CheckProb(successRate, 100):   // 百分比检定
L540182        hasChanges = true
L540184        reverseRate = GetStartRelationSuccessRate_Adored(target, self, selfToTarget, targetToSelf)
L540190        mod.NewRegularRelations.Add(target, 0x4000, random.CheckPercentProb(reverseRate))
               // succeed=true 表示对方也爱慕self（双向爱慕）

           // ===== 表白/两情相悦 (TemplateId=3, Bitmask=0x4000 特殊处理) =====
L540193    target = GetStartOrEndRelationTarget(random, 3, canStart.BoyAndGirlFriends, selfPersonalities)
L540194    if target != null:
L540195      hasChanges = true
L540197      selfToTarget = GetRelation(self.id, target.id)
L540198      targetToSelf = GetRelation(target.id, self.id)
L540199      successRate = GetStartRelationSuccessRate_BoyOrGirlFriend(self, target, selfToTarget, targetToSelf)
L540200      mod.NewBoyOrGirlFriend = (target, random.CheckPercentProb(successRate))

           // ===== 分手 (TemplateId=4, Bitmask=0x4000) =====
L540202    target = GetStartOrEndRelationTarget(random, 4, canEnd.BoyAndGirlFriends, selfPersonalities)
L540203    if target != null:
L540204      hasChanges = true
L540206      targetBehavior = target.GetBehaviorType()
L540207      isNOTMutual = NOT random.CheckPercentProb(BreakupMutuallyChance[targetBehavior])
               // isNOTMutual: 取反！true=对方不同意分手
L540213      mod.RemovedRegularRelations.Add(target, 0x4000, isNOTMutual)

           // ===== 求婚 (TemplateId=5, Bitmask=0x0400) =====
L540215    target = GetStartOrEndRelationTarget(random, 5, canStart.HusbandsAndWives, selfPersonalities)
L540216    if target != null:
L540217      hasChanges = true
L540219      selfToTarget = GetRelation(self.id, target.id)
L540220      targetToSelf = GetRelation(target.id, self.id)
L540221      successRate = GetStartRelationSuccessRate_HusbandOrWife(self, target, selfToTarget, targetToSelf)
L540227      mod.NewRegularRelations.Add(target, 0x0400, random.CheckProb(successRate, 100))

           // ===== 离婚 (TemplateId=12, Bitmask=0x0400) =====
L540229    target = GetStartOrEndRelationTarget(random, 12, canEnd.HusbandsAndWives, selfPersonalities)
L540230    if target != null:
L540231      hasChanges = true
L540238      mod.RemovedRegularRelations.Add(target, 0x0400, false)
               // 离婚总是双向

           // ===== 结友 (TemplateId=6, Bitmask=0x2000) =====
L540240    target = GetStartOrEndRelationTarget(random, 6, canStart.Friends, selfPersonalities)
L540241    if target != null:
L540242      hasChanges = true
L540249      mod.NewRegularRelations.Add(target, 0x2000, true)  // 结友总是双向

           // ===== 断友 (TemplateId=7, Bitmask=0x2000) =====
L540251    target = GetStartOrEndRelationTarget(random, 7, canEnd.Friends, selfPersonalities)
L540252    if target != null:
L540253      hasChanges = true
L540260      mod.RemovedRegularRelations.Add(target, 0x2000, false) // 断友总是双向

           // ===== 结义 (TemplateId=8, Bitmask=0x0200) =====
L540262    target = GetStartOrEndRelationTarget(random, 8, canStart.SwornBrothersAndSisters, selfPersonalities)
L540263    if target != null:
L540264      hasChanges = true
L540271      mod.NewRegularRelations.Add(target, 0x0200, true)  // 结义总是双向

           // ===== 断义 (TemplateId=9, Bitmask=0x0200) =====
L540273    target = GetStartOrEndRelationTarget(random, 9, canEnd.SwornBrothersAndSisters, selfPersonalities)
L540274    if target != null:
L540275      hasChanges = true
L540282      mod.RemovedRegularRelations.Add(target, 0x0200, false) // 断义总是双向

           // ===== 认义父母 (TemplateId=10, Bitmask=0x0040) =====
L540284    target = GetStartAdoptiveParentRelationTarget(random, 10, canStart.AdoptiveParents, selfPersonalities)
L540285    if target != null:
L540286      hasChanges = true
L540293      mod.NewRegularRelations.Add(target, 0x0040, true)

           // ===== 收义子女 (TemplateId=11, Bitmask=0x0080) =====
L540295    target = GetStartAdoptiveChildRelationTarget(random, 11, canStart.AdoptiveChildren, selfPersonalities)
L540296    if target != null:
L540297      hasChanges = true
L540304      mod.NewRegularRelations.Add(target, 0x0080, true)

           // --- 记录修改 ---
L540306    if hasChanges:
L540308      parallelRecorder.RecordType(PeriAdvanceMonthRelationsUpdate)
L540310      parallelRecorder.RecordParameterClass(mod)
L540312    释放 canStart, canEnd 对象池
```

### 2.5 收养概率计算（特殊公式）

```
L540090  function GetStartAdoptiveParentRelationTarget(random, aiTemplateId, candidates, selfPersonalities):
L540092    if candidates.Count == 0: return null
L540096    config = GetAiRelationConfig(aiTemplateId)
L540097    if NOT CheckChangeRelationTypeChance(random, selfPersonalities, config.PersonalityType): return null
L540101    targetId = candidates.GetRandom(random)
L540103    relation = GetRelation(targetId, self.id)  // 注意：是对方→自己的关系
L540104    chance = target.CurrAge - self.CurrAge      // 年龄差（对方越大越容易）
L540105    chance += (target.InteractionGrade(self) - self.InteractionGrade(target)) * 3  // 品级差*3
L540106    chance += (FavorabilityType(relation.Favorability) - 4) * 5  // (好感等级-4)*5
L540107    return random.CheckPercentProb(chance) ? target : null

L540110  function GetStartAdoptiveChildRelationTarget(random, aiTemplateId, candidates, selfPersonalities):
           // 结构与上方相同，但年龄差和品级差取反
L540124    chance = self.CurrAge - target.CurrAge      // 自己越大越容易
L540125    chance += (self.InteractionGrade(target) - target.InteractionGrade(self)) * 3
L540126    chance += (FavorabilityType(relation.Favorability) - 4) * 5
L540127    return random.CheckPercentProb(chance) ? target : null
```

### 2.6 ComplementPeriAdvanceMonth_RelationsUpdate（应用关系变更）

```
L540316  function ComplementPeriAdvanceMonth_RelationsUpdate(context, mod):
L540318    character = mod.Character
L540319    taiwuCharId = Taiwu.GetTaiwuCharId()
L540321    behaviorType = character.GetBehaviorType()
L540322    selfIsTaiwuPeople = IsTaiwuPeople(character.id)
L540323    monthlyEvents = World.GetMonthlyEventCollection()

           // --- 建立新认识 ---
L540326    if mod.NewlyMetCharacters != null:
L540328      for each newChar in mod.NewlyMetCharacters:
L540330        TryCreateGeneralRelation(context, character, newChar)

           // --- 建立新关系 ---
L540333    if mod.NewRegularRelations != null:
L540335      for each (target, relationType, succeed) in mod.NewRegularRelations:
L540340        targetIsTaiwuPeople = IsTaiwuPeople(target.id)
L540341        switch relationType:

               // 义父母 (0x0040)
L540343          case 64:
L540344            if target.id == taiwuCharId:
                    添加月报事件（被太吾收养）
                  else:
L540357              ApplyAddRelation_AdoptiveParent(context, character, target, ...)

               // 义子女 (0x0080)
L540360          case 128:
L540361            if target.id == taiwuCharId:
                    添加月报事件（收养太吾子女）
                  else:
L540374              ApplyAddRelation_AdoptiveChild(context, character, target, ...)

               // 结义 (0x0200)
L540377          case 512:
L540378            if target.id == taiwuCharId:
L540380              monthlyEvents.AddBecomeSwornBrotherOrSister(...)
                  else:
L540384              ApplyBecomeSwornBrotherOrSister(context, character, target, ...)

               // 夫妻 (0x0400)
L540387          case 1024:
L540388            if target.id == taiwuCharId:
L540390              monthlyEvents.AddProposeMarriage(...)
                  else:
L540394              ApplyBecomeHusbandOrWife(context, character, target, behaviorType,
                                              succeed, selfIsTaiwuPeople, targetIsTaiwuPeople)

               // 好友 (0x2000)
L540397          case 8192:
L540398            if target.id == taiwuCharId:
L540400              monthlyEvents.AddBecomeFriend(...)
                  else:
L540404              ApplyBecomeFriend(context, character, target, ...)

               // 爱慕 (0x4000)
L540407          case 16384:
L540408            if target.id == taiwuCharId:
L540410              monthlyEvents.AddAdore(...)
                  else:
L540414              ApplyAddRelation_Adore(context, character, target, behaviorType,
                                            succeed,   // succeed=true 表示双向爱慕
                                            selfIsTaiwuPeople, targetIsTaiwuPeople)

               // 仇敌 (0x8000)
L540417          case 32768:
L540418            if target.id == taiwuCharId:
L540420              monthlyEvents.AddMakeEnemy(...)
                  else:
L540423              ApplyAddRelation_Enemy(context, character, target, selfIsTaiwuPeople, 0)
L540424              if succeed:  // succeed=true 表示对方也结仇
L540426                ApplyAddRelation_Enemy(context, target, character, targetIsTaiwuPeople, 0)

           // --- 移除关系 ---
L540434    if mod.RemovedRegularRelations != null:
L540436      for each (target, relationType, targetStillHasRelation) in mod.RemovedRegularRelations:
L540441        targetIsTaiwuPeople = IsTaiwuPeople(target.id)
L540443        switch relationType:

               // 断义 (0x0200)
L540444          case 512:
                  if target == 太吾: 添加月报; else: ApplySeverSwornBrotherOrSister(...)

               // 离婚 (0x0400)
L540454          case 1024:
L540455            if target.id != taiwuCharId:   // 太吾离婚不在此处处理
L540457              ApplySeverHusbandOrWife(context, character, target, ...)

               // 断友 (0x2000)
L540460          case 8192:
                  if target == 太吾: 添加月报; else: ApplySeverFriend(...)

               // 分手 (0x4000)
L540470          case 16384:
                  if target == 太吾: 添加月报
                  else:
L540477            ApplyBreakupWithBoyOrGirlFriend(context, character, target, behaviorType,
                                                    targetStillHasRelation,  // true=对方不同意分手
                                                    selfIsTaiwuPeople, targetIsTaiwuPeople)

               // 解仇 (0x8000)
L540480          case 32768:
                  if target == 太吾: 添加月报
                  else:
L540486            ApplySeverEnemy(context, character, target, ...)
L540487            if NOT targetStillHasRelation:   // 对方也解除
L540489              ApplySeverEnemy(context, target, character, ...)

           // --- 表白/两情相悦（特殊流程） ---
L540497    if mod.NewBoyOrGirlFriend.targetChar != null:
L540499      (targetChar, succeed) = mod.NewBoyOrGirlFriend
L540500      if targetChar.id == taiwuCharId:
L540502        monthlyEvents.AddConfess(character.id, location, taiwuCharId)
               return
L540505      targetIsTaiwuPeople = IsTaiwuPeople(targetChar.id)
L540506      ApplyBecomeBoyOrGirlFriend(context, character, targetChar, behaviorType,
                                         succeed, selfIsTaiwuPeople, targetIsTaiwuPeople)
```

---

## 第三部分：性关系成功率公式伪代码

### 3.1 SexRelationBaseRate（基础好感率，爱慕/表白/求婚共用）

```
L558869  function GetStartRelationSuccessRate_SexRelationBaseRate(selfChar, targetChar, selfToTarget, targetToSelf):
L558871    rate = 100

           // --- 性别/性取向检查 ---
L558878    isBisexual = targetChar.GetBisexual()
L558879    sameGender  = (self.Gender == target.Gender)
L558880    sameDisplay = (self.DisplayGender == target.DisplayGender)
L558881    if (isBisexual AND 异性 AND 异显示性别) OR (非双 AND 同性 AND 同显示性别):
L558887      return MIN_VALUE (不可能)       // 性取向不匹配
L558889    if 非双 AND 同性 AND 异显示性别:
L558895      rate -= 20                      // 跨性别减成
L558897    if 双性恋 AND 异性 AND 同显示性别:
L558903      rate -= 20                      // 双性恋显示性别冲突减成

           // --- 双性恋加成 ---
L558906    if 同性 AND 双方都是双性恋:
L558908      rate += 20

           // --- 关系修正 ---
L558913    if target→self有夫妻关系(0x0400):
L558915      rate += 40                      // 夫妻之间更容易
L558918    if target→self有直系血亲关系:
L558920      rate -= 160                     // 直系血亲大幅减少
L558923    if target→self有非血亲家族关系:
L558925      rate -= 80                      // 非血亲家族减少
L558928    if self→target有师徒/同门(0x0800/0x1000):
L558930      rate -= 40                      // 师徒减少

           // --- 年龄差修正 ---
L558937    if 同性:
L558939      agePenalty = clamp(3 * abs(selfAge - targetAge), 0, 60)
           else (异性):
             // 女方年长超过10岁或男方年长时才有减成
L558945      agePenalty = 复杂公式（女性年龄超出10岁惩罚，男性年龄差惩罚）
             agePenalty = clamp(..., 0, 60)
           rate -= agePenalty

           // --- 好感度等级加成 ---
L558951    favorType = FavorabilityType(targetToSelf.Favorability)
L558952    if favorType >= 3:
L558954      rate += (favorType - 3) * 60     // 好感度每超过3级加60

           // --- 魅力差加成 ---
L558960    selfAttraction  = AttractionType(selfChar.Attraction)
L558961    targetAttraction = AttractionType(targetChar.Attraction)
L558962    rate += (selfAttraction - targetAttraction) * 10  // 自己更有魅力则加成

           // --- 品级差加成 ---
L558967    gradeDiff = selfChar.InteractionGrade(target) - targetChar.InteractionGrade(self)
L558968    rate += gradeDiff * 10

           // --- 性别偏好加成 ---
L558972    if self.DisplayGender == 0 (显示为男):
L558974      rate += 20                       // 男性追求方有加成

           // --- 非太吾NPC间加成 ---
L558979    if self != 太吾 AND target != 太吾:
L558981      rate += 20                       // NPC之间额外加成

           // --- 已有爱慕者惩罚 ---
L558984    targetAdoreCount = 目标现有存活爱慕者数量(排除self)
L558995    if targetAdoreCount > 0:
L558997      featureFactor = CalcAdoreMultiplePeopleChanceFactor(target)
L558998      perAdored = StartAdoreChanceChangePerAdored[target.BehaviorType]
L558999      rate += targetAdoreCount * perAdored * featureFactor / 100

           // --- 专情特性 ---
L559003    if target有特性685(专情) AND self非太吾知己:
L559005      return MIN_VALUE

           // --- 子女数惩罚 ---
L559013    childCount = self的子女数 + target的子女数
L559017    rate -= 10 * childCount

           // --- 出家惩罚 ---
L559022    if self.MonkType != 0: rate -= 80
L559027    if target.MonkType != 0: rate -= 80

           // --- 不育惩罚 ---
L559034    if self.Fertility <= 0: rate -= 30
L559039    if target.Fertility <= 0: rate -= 30
L559045    return rate
```

### 3.2 爱慕成功率

```
L558787  function GetStartRelationSuccessRate_Adored(selfChar, targetChar, selfToTarget, targetToSelf):
           // 注意：参数顺序反转传入 BaseRate
L558789    rate = GetStartRelationSuccessRate_SexRelationBaseRate(targetChar, selfChar, targetToSelf, selfToTarget)
L558790    if rate == MIN_VALUE: return 0
L558794    if selfToTarget有直系血亲关系:
L558796      rate = rate * IncestSexRelationChanceFactor[selfBehavior] / 100
L558799    return rate
```

### 3.3 表白成功率

```
L558802  function GetStartRelationSuccessRate_BoyOrGirlFriend(selfChar, targetChar, selfToTarget, targetToSelf):
L558821    rate = GetStartRelationSuccessRate_SexRelationBaseRate(selfChar, targetChar, selfToTarget, targetToSelf)
L558822    if rate == MIN_VALUE: return 0
L558826    rate -= 20                         // 基础扣减
           // --- 已婚惩罚 ---
L558827    selfSpouse = GetAliveSpouse(self.id)
L558828    targetSpouse = GetAliveSpouse(target.id)
L558830    if selfSpouse存在 AND selfSpouse != target:
L558833      rate -= 60
L558835    if targetSpouse存在 AND targetSpouse != self:
L558838      rate -= 60
           // --- AI调整 ---
L558840    rate += GetAiActionSuccessRateAdjust(self.id, target.id, 10, 爱慕类型ID)
           // --- 近亲修正 ---
L558841    if targetToSelf有直系血亲:
L558844      rate = rate * IncestSexRelationChanceFactor[targetBehavior] / 100
           // --- 太吾专属加成 ---
L558846    if self.IsTaiwu():
L558848      rate *= TaiwuPropertyPermanentBonus(ConfessingLoveSuccessOdds)
L558856    return rate
```

### 3.4 求婚成功率

```
L558859  function GetStartRelationSuccessRate_HusbandOrWife(selfChar, targetChar, selfToTarget, targetToSelf):
L558861    rate = GetStartRelationSuccessRate_SexRelationBaseRate(selfChar, targetChar, selfToTarget, targetToSelf)
L558862    if rate == MIN_VALUE: return 0
L558866    return rate - 40                   // 比爱慕多扣40
```

---

## 第四部分：远程婚配系统伪代码

### 4.1 UpdateDistantMarriages（入口）

```
L497826  function UpdateDistantMarriages(context):
L497828    _taiwuRelatedDistanceMarriages.Clear()
L497829    passCondition = 0
L497830    passRateCheck = 0
L497831    foundTarget = 0
L497832    for each (charId, character) in _objects:
L497837      marriageAgeGroup = character.GetMarriageAgeGroup()
L497838      if NOT CanStartDistantMarriage(character): continue
L497842      passCondition++
L497843      chance = marriageAgeGroup.GetDistantMarriageChance()
               // GoodAge -> GlobalConfig.DistantMarriageGoodAgeChance
               // LateAge -> GlobalConfig.DistantMarriageLateAgeChance
               // ExceedAge -> 0 (不能远婚)
L497844      if random.CheckPercentProb(chance):
L497846        passRateCheck++
L497847        if UpdateDistantMarriage(context, character):
L497849          foundTarget++
L497853    Log("[DistantMarriage] PassConditionCount={passCondition}, "
              "PassSuccessRateCheckCount={passRateCheck}, FoundTargetCount={foundTarget}")
```

### 4.2 CanStartDistantMarriage（前置条件）

```
L498135  function CanStartDistantMarriage(character):
L498137    if NOT character.IsInteractableAsIntelligentCharacter(): return false
L498141    orgInfo = character.GetOrganizationInfo()
L498142    if orgInfo.OrgTemplateId == 16 (太吾村): return false
           if orgInfo.OrgTemplateId == 0 (无门无派): return false
           if orgInfo.SettlementId < 0: return false
           if NOT orgInfo.Principal: return false       // 必须是据点主要成员
           if Organization.IsSect(orgInfo.OrgTemplateId): return false  // 门派弟子不参与远婚
           if orgInfo.GetOrgMemberConfig().ChildGrade < 0: return false // 不能生育的组织
L498146    if NOT MapAreaData.IsRegularArea(character.Location.AreaId): return false
L498150    return GetAliveSpouse(character.id) < 0      // 无存活配偶
           AND character.MonkType == 0                  // 非出家人
           AND NOT character.GetBisexual()              // 非双性恋
           AND NOT character.IsInTaiwuGroup()           // 不在太吾队伍中
           AND NOT IsTemporaryIntelligentCharacter(character.id) // 非临时角色
```

### 4.3 婚龄分组

```
L553225  function GetMarriageAgeGroup(age):
L553227    if age in GlobalConfig.GoodMarriageAgeRange: return GoodAge
L553231    if age in GlobalConfig.LateMarriageAgeRange: return LateAge
L553235    return ExceedAge   // 超龄，概率为0
```

### 4.4 UpdateDistantMarriage（单角色处理）

```
L497856  function UpdateDistantMarriage(context, character):
L497858    (target, marriageType) = SelectDistantMarriageTarget(context, character)
L497859    if target == null: return false
L497863    selfId = character.GetId()
L497864    targetId = target.GetId()
           // --- 太吾高好感角色需要征询 ---
L497865    if TryGetRelation(selfId, taiwuCharId, relation) AND relation.FavorabilityType >= 4:
L497867      _taiwuRelatedDistanceMarriages.Add(selfId, (targetId, marriageType))
L497868      monthlyEvents.AddAiLongDistanceMarriageAskAdvice(selfId)
L497869      return false   // 暂不执行，等太吾决定
L497871    ProcessDistantMarriage(context, selfId, targetId, marriageType, favorDelta=0)
L497872    return true
```

### 4.5 SelectDistantMarriageTarget（搜索目标，三级范围）

```
L498041  function SelectDistantMarriageTarget(context, character):
L498043    orgInfo = character.GetOrganizationInfo()
L498044    settlement = Organization.GetSettlement(orgInfo.SettlementId)
           // 第一优先：同地区（同Area不同Settlement）
L498045    target = SelectDistantMarriageTargetSameArea(context, character, settlement)
L498046    if target != null: return (target, SameArea)
           // 第二优先：同州（同State不同Area）
L498050    target = SelectDistantMarriageTargetSameState(context, character, settlement)
L498051    if target != null: return (target, SameState)
           // 第三优先：邻州
L498055    target = SelectDistantMarriageTargetNeighborStates(context, character, settlement)
L498056    if target != null: return (target, NeighborStates)
L498060    return (null, None)
```

### 4.6 CanBeDistantMarriageTarget（目标资格）

```
L498153  function CanBeDistantMarriageTarget(selfChar, targetChar):
L498155    if selfChar.Gender == targetChar.Gender: return false  // 必须异性
L498159    if NOT CanStartDistantMarriage(targetChar): return false // 目标也须满足前置条件
L498163    selfAgeGroup = selfChar.GetMarriageAgeGroup()
L498164    if NOT selfAgeGroup.CheckMarriageTargetGradeInRange(
                selfChar.InteractionGrade(target), target.InteractionGrade(self)):
             return false
             // GoodAge: (targetGrade - selfGrade) in GlobalConfig.DistantMarriageGoodAgeTargetGradeRange
             // LateAge: (targetGrade - selfGrade) in GlobalConfig.DistantMarriageLateAgeTargetGradeRange
L498168    if NOT selfAgeGroup.CheckMarriageTargetAgeInRange(target.Gender, target.CurrAge):
             return false
             // GoodAge: target年龄 in GlobalConfig.DistantMarriageGoodAgeTargetAgeRange[targetGender]
             // LateAge: target年龄 in GlobalConfig.DistantMarriageLateAgeTargetAgeRange[targetGender]
L498174    if NOT AllowAddingHusbandOrWifeRelation(selfId, targetId): return false
L498178    if HasEnemyRelationWithFamily(selfId, targetId): return false // 家族仇恨
L498179    if HasEnemyRelationWithFamily(targetId, selfId): return false
L498182    return true
```

### 4.7 CompareCharacterForDistantMarriageTarget（目标排序）

```
L498084  function CompareCharacterForDistantMarriageTarget(selfChar, first, second):
           // 优先级 1: 品级
L498086    gradeA = first.InteractionGrade(second)
L498087    gradeB = second.InteractionGrade(first)
L498088    if gradeA != gradeB: return gradeA.CompareTo(gradeB)   // 品级高者优先
           // 优先级 2: 年龄（年轻者优先）
L498092    ageA = first.CurrAge
L498093    ageB = second.CurrAge
L498094    if ageA != ageB: return ageB.CompareTo(ageA)           // 年龄小者优先
           // 优先级 3: 表白成功率
L498117    rateA = GetStartRelationSuccessRate_BoyOrGirlFriend(self, first, ...)
L498118    rateB = GetStartRelationSuccessRate_BoyOrGirlFriend(self, second, ...)
L498119    if rateA != rateB: return rateA.CompareTo(rateB)       // 成功率高者优先
           // 优先级 4: ID (deterministic tiebreaker)
L498123    return firstId.CompareTo(secondId)
```

### 4.8 ProcessDistantMarriage（执行婚配）

```
L497875  function ProcessDistantMarriage(context, selfCharId, targetCharId, marriageType, favorDelta=0):
L497877    selfChar = GetElement(selfCharId)
L497878    targetChar = GetElement(targetCharId)
L497879    selfSettlement = selfChar.OrgInfo.SettlementId
L497880    targetSettlement = targetChar.OrgInfo.SettlementId
L497881    currDate = World.GetCurrDate()
           // --- 生平记录 ---
L497884    switch marriageType:
L497886      case SameArea:
               lifeRecord.AddSameAreaDistantMarriage(双方各一条)
               if 任一方是太吾村民: 月报通知
L497898      case SameState:
               lifeRecord.AddSameStateDistantMarriage(双方各一条)
               if 任一方是太吾村民: 月报通知
L497910      case NeighborStates:
               lifeRecord.AddDifferentStateDistantMarriage(双方各一条)
               if 任一方是太吾村民: 月报通知
           // --- 建立婚姻 ---
L497923    selfChar.AddGoal(context, 272, targetCharId)      // 添加婚姻目标
L497924    targetChar.AddGoal(context, 272, selfCharId)
L497925    AddHusbandOrWifeRelations(context, selfCharId, targetCharId, currDate)
L497926    Organization.UpdateOrganizationAfterMarriage(context, selfChar, targetChar)
           // --- 好感度调整（仅太吾决策时使用） ---
L497927    if favorDelta != 0:
L497929      ChangeFavorabilityOptional(context, selfChar, targetChar, favorDelta, -1)
```

---

## 第五部分：婚内爱慕系统伪代码

### 5.1 UpdateAdoreRelationsInMarriage（入口）

```
L498185  function UpdateAdoreRelationsInMarriage(context):
L498187    candidatePairs = new HashSet<RelationKey>()
L498188    for each (charId, character) in _objects:
L498190      if NOT character.IsInteractableAsIntelligentCharacter(): continue
L498194      spouseId = GetAliveSpouse(charId)
L498195      if spouseId < 0: continue        // 无配偶跳过
L498197      pairKey = RelationKey(spouseId, charId)
L498198      if NOT candidatePairs.Contains(pairKey)   // 避免重复处理同对夫妻
             AND random.CheckPercentProb(GlobalConfig.HusbandAndWifeStartAdoreChance)
             AND NOT IsAiActionInCooldown(charId, 4, 0)    // actionType=4 subType=0
             AND NOT IsAiActionInCooldown(spouseId, 4, 0)
             AND (NOT HasRelation(charId, spouseId, 0x4000) // 至少一方尚无爱慕关系
                  OR NOT HasRelation(spouseId, charId, 0x4000)):
               // 注意：源码中两个条件都检查了同方向，疑似bug
               // 原代码: !HasRelation(num2, aliveSpouse, 16384) || !HasRelation(num2, aliveSpouse, 16384)
               // 实际效果: 只要 charId->spouseId 无爱慕就满足
L498200        candidatePairs.Add(RelationKey(charId, spouseId))

           // --- 处理候选夫妻对 ---
L498204    cooldown = GlobalConfig.HusbandAndWifeStartAdoreCooldown
L498205    successCount = 0
L498206    for each pair in candidatePairs:
L498208      AddAiActionCooldown(context, pair.CharId, 4, 0, cooldown)
L498209      AddAiActionCooldown(context, pair.RelatedCharId, 4, 0, cooldown)
L498210      charA = _objects[pair.CharId]
L498211      charB = _objects[pair.RelatedCharId]
L498212      relationAtoB = GetRelation(pair.CharId, pair.RelatedCharId)
L498213      relationBtoA = GetRelation(pair.RelatedCharId, pair.CharId)
             // 尝试 A -> B 方向爱慕
L498214      successA = NOT HasRelation(relationAtoB, 0x4000)
                        AND UpdateOneWayAdoreRelationInMarriage(context, charA, charB, relationAtoB, relationBtoA)
             // 尝试 B -> A 方向爱慕
L498215      successB = NOT HasRelation(relationBtoA, 0x4000)
                        AND UpdateOneWayAdoreRelationInMarriage(context, charB, charA, relationBtoA, relationAtoB)
L498216      if successA OR successB: successCount++
L498221    Log("Adore Relations In Marriage: {successCount}/{candidatePairs.Count}")
```

### 5.2 UpdateOneWayAdoreRelationInMarriage（单方向爱慕检定）

```
L498224  function UpdateOneWayAdoreRelationInMarriage(context, selfChar, targetChar, selfToTarget, targetToSelf):
           // 使用与普通爱慕相同的成功率公式
L498226    rate = GetStartRelationSuccessRate_Adored(selfChar, targetChar, selfToTarget, targetToSelf)
L498227    if NOT random.CheckPercentProb(rate): return false
           // --- 直接建立单向爱慕关系 ---
L498231    selfId = selfChar.GetId()
L498232    targetId = targetChar.GetId()
L498233    currDate = World.GetCurrDate()
L498234    AddRelation(context, selfId, targetId, 0x4000, currDate)
L498236    lifeRecord.AddAdoreInMarriage(selfId, currDate, targetId)
L498237    selfChar.AddGoal(context, 272, targetId)
L498238    return true
```

---

## 第六部分：参考表

### 关系显示类型

| ID | 游戏显示名 |
|----|-----------|
| 0 | 父母 |
| 1 | 结义 |
| 2 | 夫妻 |
| 3 | 子女 |
| 4 | 派系 |
| 5 | 仇敌 |
| 6 | 朋友 |
| 7 | 爱慕 |
| 8 | 师承 |
| 9 | 手足 |
| 10-12 | 血亲父母/子女/手足 |
| 13-15 | 继亲父母/子女/手足 |
| 16-18 | 义亲父母/子女/手足 |

### 结仇类型

| ID | 游戏名 | 说明 |
|----|--------|------|
| 0 | 未知原因 | 月度AI随机 |
| 1 | 劫持 | 囚犯好感过低 |
| 2 | 表白失败 | 表白被拒后仇恨 |
| 3 | 分手 | 分手/离婚后仇恨 |
| 4 | 求婚失败 | 求婚被拒后仇恨 |
| 5 | 秘闻公开 | 情报系统揭发 |
| 6 | 魑魅蛊 | 妖怪相关 |
| 7 | 出手袭击 | 攻击行为 |
| 8 | 逐出太吾村 | 驱逐村民 |
| 9 | 情难自禁 | 强制侮辱 |

### 月报事件 ID

| ID | 名称 | 游戏描述 |
|----|------|----------|
| 43 | 结下仇怨 | {0}在{1}对{2}怀恨在心，结下了莫名的仇怨。 |
| 44 | 化解仇怨 | {0}在{1}化解了对{2}的仇恨，不再视其为敌人了。 |
| 45 | 心生爱慕 | {0}在{1}对{2}心生爱慕之情。 |
| 46 | 表露心事 | {0}在{1}向{2}表明心中爱意... |
| 47 | 恋人分手 | {0}在{1}向{2}提议断绝彼此的情爱... |
| 48 | 共结连理 | {0}在{1}向{2}提议互许终生，结为夫妻... |
| 49 | 结为好友 | {0}在{1}与{2}相谈甚欢，提议结为知己好友... |
| 50 | 断绝友谊 | {0}在{1}与{2}产生隔阂，不复往日友谊... |
| 51 | 义结金兰 | {0}在{1}向{2}提议共结金兰之契... |
| 52 | 割袍断义 | {0}在{1}向{2}提出割袍断义... |
| 122 | 门派责罚 | {0}因与太吾传人的情谊受到了门派责罚...（模板存在但未被代码调用） |
